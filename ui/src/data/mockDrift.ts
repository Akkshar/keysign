/**
 * The Drift view has no mock metrics. It reads public/drift.json, built by
 * pipeline/drift.py from public longitudinal data and our own days. Kept so
 * imports resolve.
 */
export const mockDriftMetrics = {} as const;
