# Raw keystroke data is personal. Keep it out of git by default.
data/samples/*.json
data/samples/*.csv
*.local.json

# Python
__pycache__/
*.pyc
.venv/
venv/

# Node
node_modules/
dist/
build/

# Editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Derived feature tables
data/*.csv

# External datasets (staged from zips, see data/external/README.md)
data/external/*
!data/external/README.md

# Personal baselines are derived from personal data
data/baselines/

# Trained models (derived from personal data)
data/models/

# ad-hoc replay files
capture/replay*.json

# Silent alert log and intruder webcam frames (personal)
data/alerts*.jsonl
data/alert_photos/
data/faces/

# Snapshots of the above (pipeline.snapshot)
data/backups/

# Sign-in: local email -> typing-profile links, and the Firebase web config
data/accounts.json
data/settings.json
ui/.env
ui/.env.local

# Live session recordings (raw keystrokes, personal)
data/sessions/

# Playwright screenshots from ui/tests
ui/tests/out/

# Face models (39 MB, fetch with: uv run python -m backend.faces fetch)
backend/assets/*.onnx
backend/assets/*.part

# Which account a browser on this machine last signed in as (the app window reads it)
data/active_account.json
